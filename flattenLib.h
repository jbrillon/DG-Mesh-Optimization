#ifndef FLATTENLIB_H
#define FLATTENLIB_H

#include <iostream>
#include <stdlib.h>
#include <stdio.h>
#include <math.h>

void flattenF(int, int, double**, double*);
void unflattenF(int, int, double**, double*);
void flattenC(int, int, double**, double*);
void unflattenC(int, int, double**, double*);

#endif