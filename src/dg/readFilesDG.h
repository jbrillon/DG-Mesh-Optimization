#ifndef READFILESDG_H
#define READFILESDG_H

#include <iostream>
#include <fstream> // for writing to files
#include <string> // for strings
#include <stdlib.h>
#include <stdio.h>

void readSetupFile(std::string);
void readElementVertices(std::string);
void readArctanParameters(std::string);

#endif